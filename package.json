{
  "name": "laser-mirror-commercial",
  "private": true,
  "version": "7.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode web",
    "build:web": "vite build --mode web",
    "build:wechat": "node tools/build-wechat.mjs",
    "build:douyin": "vite build --mode douyin && node scripts/package-platform.mjs douyin",
    "build:xhs": "vite build --mode xhs && node scripts/package-platform.mjs xhs",
    "build:all": "npm run validate:levels && npm run build:web && npm run build:wechat && npm run build:douyin && npm run build:xhs",
    "validate:levels": "node scripts/validate-levels.mjs",
    "verify:expansion": "npx tsx --tsconfig tsconfig.json scripts/solve-expansion.ts",
    "typecheck": "tsc --noEmit",
    "gm": "vite --config tools/gm/vite.config.ts --host 0.0.0.0 --port 8350 --strictPort --open"
  },
  "dependencies": {
    "pixi.js": "8.19.0"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "8.1.0",
    "@types/node": "22.15.30"
  }
}